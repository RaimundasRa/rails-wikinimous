<h1><%= @article.title %></h1>
<br>
<p><%= @article.content %></p>
<br>

<%= link_to "Go back to articles", articles_path %>

<%= link_to "Edit article", edit_article_path(@article) %>

<%= link_to "Delete article", article_path(@article), method: :delete, data: { confirm: 'You sure you want to delete?' } %>
